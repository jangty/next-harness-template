# Data — 데이터 접근·마이그레이션

> **슬롯.** Supabase(Postgres) + Prisma 기준. 구체 절차는 개발하며 채운다.

## 데이터 접근 패턴
_(쿼리는 어디서/어떻게 — server action? route handler? 접근 계층 — 채울 것)_

## 마이그레이션 절차
- ⚠️ 마이그레이션 **적용**은 고위험 → 사람 게이트(권한 `ask`).
- _(prisma migrate 흐름, 생성 vs 적용 구분, 롤백 — 채울 것)_

## 스키마
_(prisma/schema.prisma 위치·컨벤션 — 채울 것)_
