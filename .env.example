# 환경변수 예시 — 실제 값은 .env 에 (커밋 금지, 가드 훅이 차단).
# 실제 변수 목록은 스택(Supabase/Prisma)이 붙으면 src/env.ts 스키마와 함께 채운다.

NODE_ENV=development

# Supabase (값 채울 때 docs/security.md 의 비밀 취급 모델을 따를 것)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=

# Prisma
# DATABASE_URL=
